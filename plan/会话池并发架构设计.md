# 会话池并发架构设计

## Context

当前远程执行框架采用「单服务器单会话」模型：每个服务器（由 `transport.key` 标识）共享一个 `QueuedRemoteSession`，通过 `asyncio.Lock` 串行化所有命令。这意味着同一服务器上的训练启动（可能耗时数小时）会阻塞所有其他操作（状态查询、日志读取等）。

Benchmark 实测数据（2026-05-25，目标服务器 47.94.137.97:30009）：
- 10 个 Jupyter Terminal 并发创建：3.3s，成功率 100%
- 环境变量/工作目录/进程/PTY 输出：全部隔离
- 10 会话并发执行 vs 单会话串行：加速比 7.0x
- 单次命令执行仅 ~40ms，会话创建/销毁开销 ~1500ms

结论：**会话池架构完全可行，且能将请求延迟从 1500ms（每次创建）降至 ~40ms（池化复用）**。

## 设计参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 每服务器初始会话数 | 5 | 服务启动时预热 |
| 每服务器最大会话数 | 20 | 超出后排队等待 |
| 探活间隔 | 60s | 每 60s 对空闲会话发送 ping |
| 空闲超时 | 30min | 超过 30min 未使用的会话自动销毁 |
| 缩容下限 | 5 | 空闲缩容不低于初始数 |

## 架构概览

```
┌─────────────────────────────────────────────────────────────────────┐
│  Consumer Layer (不变)                                               │
│  execution_service.py / server_service.py / executions.py           │
│  调用: remote_executor.run_for_server(server, script, timeout)      │
├─────────────────────────────────────────────────────────────────────┤
│  SessionPoolManager (新，替代 RemoteExecutorFactory)                 │
│  ┌───────────────────────────────────────────────────────────┐      │
│  │  _pools: dict[str, SessionPool]                           │      │
│  │  key = transport.key                                      │      │
│  │                                                           │      │
│  │  run_for_server() → get_or_create_pool() → pool.execute() │      │
│  │  run_for_draft()  → get_or_create_pool() → pool.execute() │      │
│  └───────────────────────────────────────────────────────────┘      │
├─────────────────────────────────────────────────────────────────────┤
│  SessionPool (新，每服务器一个)                                       │
│  ┌───────────────────────────────────────────────────────────┐      │
│  │  _idle: list[SessionEntry]     # 空闲会话                  │      │
│  │  _in_use: set[SessionEntry]    # 使用中会话                │      │
│  │  _waiters: asyncio.Queue       # 排队等待者                │      │
│  │                                                           │      │
│  │  acquire() → 获取空闲会话 or 创建新会话 or 排队等待         │      │
│  │  release() → 归还 or 销毁                                 │      │
│  │  _health_loop() → 定期 ping 探活                          │      │
│  │  _shrink_loop() → 空闲超时缩容                             │      │
│  └───────────────────────────────────────────────────────────┘      │
├─────────────────────────────────────────────────────────────────────┤
│  SessionEntry (新)                                                  │
│  ┌───────────────────────────────────────────────────────────┐      │
│  │  session: QueuedRemoteSession   # 原有的会话封装           │      │
│  │  last_used_at: float            # 最后使用时间             │      │
│  │  created_at: float              # 创建时间                 │      │
│  │  is_healthy: bool               # 最近探活结果             │      │
│  └───────────────────────────────────────────────────────────┘      │
├─────────────────────────────────────────────────────────────────────┤
│  QueuedRemoteSession (不变)                                         │
│  负责: marker 注入、JSON 提取、超时控制、错误 reset                   │
├─────────────────────────────────────────────────────────────────────┤
│  Transport (不变)                                                   │
│  JupyterTransport / SshTransport                                    │
└─────────────────────────────────────────────────────────────────────┘
```

## 核心流程

### 请求执行流程

```
remote_executor.run_for_server(server, script, timeout_ms)
  │
  ├── 1. config = _config_for_server(server)
  ├── 2. key = _key_for_config(config)         # e.g. "jupyter:http://host:port"
  ├── 3. pool = _get_or_create_pool(key, config)
  │       └── 如果 pool 不存在，创建并 warmup(5)
  │
  └── 4. return await pool.execute(script, timeout_ms)
            │
            ├── entry = await pool.acquire()
            │     │
            │     ├── [快速路径] _idle 非空 → 取出 entry，更新 last_used_at
            │     │     └── 检查 entry.is_healthy，不健康则销毁重建
            │     │
            │     ├── [扩展路径] total < max_size → 创建新 entry
            │     │     └── _create_session(config) → warmup 单个会话
            │     │
            │     └── [排队路径] total == max_size → 等待 _waiters queue
            │           └── asyncio.wait_for(waiter.get(), timeout=queue_timeout)
            │
            ├── try:
            │     result = await entry.session.run_script(script, timeout_ms)
            │
            ├── finally:
            │     pool.release(entry)
            │       │
            │       ├── 检查 entry.session.transport.is_connected
            │       │     ├── True → 放回 _idle
            │       │     └── False → 销毁，_create_replacement()
            │       │
            │       └── 如果有 waiter 在排队 → 直接交给 waiter
            │
            └── return result
```

### 探活流程 (每 60s)

```
_health_loop:
  for entry in list(_idle):
      try:
          await entry.session.run_script('echo "ping"', timeout_ms=5000)
          entry.is_healthy = True
      except Exception:
          entry.is_healthy = False
          _destroy_entry(entry)
          # 补充到 min_size
          if total < min_size:
              await _create_session()
```

### 空闲缩容流程 (每 60s)

```
_shrink_loop:
  now = time.monotonic()
  for entry in sorted(_idle, key=lambda e: e.last_used_at):
      if total <= min_size:
          break
      if now - entry.last_used_at > idle_timeout (30min):
          _destroy_entry(entry)
```

## 代码改动范围

### 新增文件

| 文件 | 职责 |
|------|------|
| `backend/app/executors/session_pool.py` | `SessionPool` + `SessionEntry` + `SessionPoolManager` 核心实现 |

### 修改文件

| 文件 | 改动内容 |
|------|---------|
| `backend/app/core/config.py` | 新增 5 个池配置项 |
| `backend/app/executors/factory.py` | 重构 `RemoteExecutorFactory`，内部委托给 `SessionPoolManager` |
| `backend/app/main.py` | 添加 FastAPI lifespan，关闭时 shutdown |
| `backend/app/executors/queued_session.py` | 无改动（`transport` 属性已公开） |
| `backend/app/executors/jupyter_terminal/executor.py` | 重构为使用 pool（debug 端点） |

### 不修改的文件

| 文件 | 原因 |
|------|------|
| `app/executors/transport.py` | Transport 协议不变 |
| `app/executors/base.py` | Result/Error 类型不变 |
| `app/executors/jupyter_terminal/transport.py` | JupyterTransport 实现不变 |
| `app/executors/ssh/transport.py` | SshTransport 实现不变 |
| `app/services/execution_service.py` | 调用接口不变 |
| `app/services/server_service.py` | 调用接口不变 |
| `app/api/routes/executions.py` | 调用接口不变 |

## 详细实现

### 1. config.py 新增配置

```python
pool_min_size: int = 5                    # 每服务器初始/最小会话数
pool_max_size: int = 20                   # 每服务器最大会话数
pool_health_check_interval_s: int = 60    # 探活间隔（秒）
pool_idle_timeout_s: int = 1800           # 空闲超时（秒，30 分钟）
pool_queue_timeout_s: int = 300           # 排队等待超时（秒，5 分钟）
```

### 2. session_pool.py 核心实现

```python
from __future__ import annotations
import asyncio
import logging
import time
from dataclasses import dataclass, field

from app.executors.base import RemoteExecutionResult
from app.executors.queued_session import QueuedRemoteSession

logger = logging.getLogger(__name__)


@dataclass
class SessionEntry:
    """池中单个会话的元数据包装"""
    session: QueuedRemoteSession
    last_used_at: float = field(default_factory=time.monotonic)
    created_at: float = field(default_factory=time.monotonic)
    is_healthy: bool = True


class SessionPool:
    """单服务器会话池"""

    def __init__(
        self,
        transport_factory,          # callable → RemoteTransport
        min_size: int = 5,
        max_size: int = 20,
        health_check_interval_s: int = 60,
        idle_timeout_s: int = 1800,
        queue_timeout_s: int = 300,
    ):
        self._transport_factory = transport_factory
        self._min_size = min_size
        self._max_size = max_size
        self._health_check_interval_s = health_check_interval_s
        self._idle_timeout_s = idle_timeout_s
        self._queue_timeout_s = queue_timeout_s

        self._idle: list[SessionEntry] = []
        self._in_use: set[SessionEntry] = set()
        self._waiters: asyncio.Queue[asyncio.Event] = asyncio.Queue()
        self._lock = asyncio.Lock()
        self._closed = False
        self._health_task: asyncio.Task | None = None
        self._shrink_task: asyncio.Task | None = None

    @property
    def total(self) -> int:
        return len(self._idle) + len(self._in_use)

    async def warmup(self) -> None:
        """预热：并发创建 min_size 个会话"""
        tasks = [self._create_entry() for _ in range(self._min_size)]
        entries = await asyncio.gather(*tasks, return_exceptions=True)
        success = 0
        for entry in entries:
            if isinstance(entry, Exception):
                logger.warning(f"Warmup session failed: {entry}")
            else:
                self._idle.append(entry)
                success += 1
        logger.info(f"Pool warmup: {success}/{self._min_size} sessions ready")
        self._health_task = asyncio.create_task(self._health_loop())
        self._shrink_task = asyncio.create_task(self._shrink_loop())

    async def execute(self, script: str, timeout_ms: int | None = None) -> RemoteExecutionResult:
        """获取会话 → 执行 → 归还"""
        entry = await self.acquire()
        try:
            return await entry.session.run_script(script, timeout_ms=timeout_ms)
        finally:
            await self.release(entry)

    async def acquire(self) -> SessionEntry:
        """获取可用会话"""
        async with self._lock:
            # 快速路径：从空闲池取
            while self._idle:
                entry = self._idle.pop(0)
                if entry.is_healthy and entry.session.transport.is_connected:
                    entry.last_used_at = time.monotonic()
                    self._in_use.add(entry)
                    return entry
                await self._destroy_entry_unlocked(entry)

            # 扩展路径：未达上限，创建新会话
            if self.total < self._max_size:
                entry = await self._create_entry()
                entry.last_used_at = time.monotonic()
                self._in_use.add(entry)
                return entry

        # 排队路径：已达上限，等待归还
        waiter = asyncio.Event()
        await self._waiters.put(waiter)
        try:
            await asyncio.wait_for(waiter.wait(), timeout=self._queue_timeout_s)
        except asyncio.TimeoutError:
            raise RuntimeError(f"Session pool queue timeout ({self._queue_timeout_s}s)")

        async with self._lock:
            if self._idle:
                entry = self._idle.pop(0)
                entry.last_used_at = time.monotonic()
                self._in_use.add(entry)
                return entry
            raise RuntimeError("Session pool: waiter signaled but no session available")

    async def release(self, entry: SessionEntry) -> None:
        """归还会话"""
        async with self._lock:
            self._in_use.discard(entry)
            entry.last_used_at = time.monotonic()

            if entry.session.transport.is_connected:
                # 优先交给等待者
                while not self._waiters.empty():
                    try:
                        waiter = self._waiters.get_nowait()
                        waiter.set()
                        self._idle.append(entry)
                        return
                    except asyncio.QueueEmpty:
                        break
                self._idle.append(entry)
            else:
                await self._destroy_entry_unlocked(entry)
                if self.total < self._min_size:
                    try:
                        new_entry = await self._create_entry()
                        self._idle.append(new_entry)
                    except Exception as exc:
                        logger.warning(f"Failed to replenish session: {exc}")

    async def _create_entry(self) -> SessionEntry:
        transport = self._transport_factory()
        session = QueuedRemoteSession(transport)
        await session.ensure_connected()
        return SessionEntry(session=session)

    async def _destroy_entry_unlocked(self, entry: SessionEntry) -> None:
        entry.is_healthy = False
        try:
            await entry.session.reset()
        except Exception:
            pass

    async def _health_loop(self) -> None:
        """后台探活循环"""
        while not self._closed:
            await asyncio.sleep(self._health_check_interval_s)
            if self._closed:
                break
            for entry in list(self._idle):
                if self._closed:
                    break
                try:
                    await entry.session.run_script('echo "ping"', timeout_ms=5000)
                    entry.is_healthy = True
                except Exception:
                    entry.is_healthy = False
                    async with self._lock:
                        if entry in self._idle:
                            self._idle.remove(entry)
                            await self._destroy_entry_unlocked(entry)
                    async with self._lock:
                        if self.total < self._min_size:
                            try:
                                new_entry = await self._create_entry()
                                self._idle.append(new_entry)
                            except Exception as exc:
                                logger.warning(f"Health check replenish failed: {exc}")

    async def _shrink_loop(self) -> None:
        """后台空闲缩容循环"""
        while not self._closed:
            await asyncio.sleep(self._health_check_interval_s)
            if self._closed:
                break
            now = time.monotonic()
            async with self._lock:
                to_remove = []
                for entry in sorted(self._idle, key=lambda e: e.last_used_at):
                    if self.total - len(to_remove) <= self._min_size:
                        break
                    if now - entry.last_used_at > self._idle_timeout_s:
                        to_remove.append(entry)
                for entry in to_remove:
                    self._idle.remove(entry)
                    await self._destroy_entry_unlocked(entry)
                    logger.info(f"Pool shrunk idle session (total={self.total})")

    async def shutdown(self) -> None:
        """关闭池，销毁所有会话"""
        self._closed = True
        if self._health_task:
            self._health_task.cancel()
        if self._shrink_task:
            self._shrink_task.cancel()
        async with self._lock:
            for entry in list(self._idle) + list(self._in_use):
                await self._destroy_entry_unlocked(entry)
            self._idle.clear()
            self._in_use.clear()


class SessionPoolManager:
    """全局会话池管理器，每服务器一个 SessionPool"""

    def __init__(self):
        self._pools: dict[str, SessionPool] = {}
        self._lock = asyncio.Lock()

    async def run_for_server(self, server, script, timeout_ms=None) -> RemoteExecutionResult:
        from app.executors.factory import _config_for_server, _key_for_config, _transport_factory_for_config
        config = _config_for_server(server)
        key = _key_for_config(config)
        pool = await self._get_or_create_pool(key, lambda: _transport_factory_for_config(config))
        return await pool.execute(script, timeout_ms=timeout_ms)

    async def run_for_draft(self, config, script, timeout_ms=None) -> RemoteExecutionResult:
        from app.executors.factory import _key_for_config, _transport_factory_for_config
        key = _key_for_config(config)
        pool = await self._get_or_create_pool(key, lambda: _transport_factory_for_config(config))
        return await pool.execute(script, timeout_ms=timeout_ms)

    async def _get_or_create_pool(self, key: str, transport_factory) -> SessionPool:
        if key in self._pools:
            return self._pools[key]
        async with self._lock:
            if key in self._pools:
                return self._pools[key]
            from app.core.config import get_settings
            s = get_settings()
            pool = SessionPool(
                transport_factory=transport_factory,
                min_size=s.pool_min_size,
                max_size=s.pool_max_size,
                health_check_interval_s=s.pool_health_check_interval_s,
                idle_timeout_s=s.pool_idle_timeout_s,
                queue_timeout_s=s.pool_queue_timeout_s,
            )
            await pool.warmup()
            self._pools[key] = pool
            return pool

    async def remove_pool(self, key: str) -> None:
        async with self._lock:
            pool = self._pools.pop(key, None)
            if pool:
                await pool.shutdown()

    async def shutdown_all(self) -> None:
        async with self._lock:
            for pool in self._pools.values():
                await pool.shutdown()
            self._pools.clear()

    def get_pool_stats(self) -> dict[str, dict]:
        return {
            key: {
                "idle": len(pool._idle),
                "in_use": len(pool._in_use),
                "total": pool.total,
                "waiters": pool._waiters.qsize(),
            }
            for key, pool in self._pools.items()
        }
```

### 3. factory.py 重构

重构 `RemoteExecutorFactory` 为外观类，内部委托给 `SessionPoolManager`。

提取 `_config_for_server()`、`_key_for_config()`、`_transport_factory_for_config()` 为模块级函数，供 `SessionPoolManager` 调用。

**向后兼容保证**：
- `remote_executor` 单例名保留
- `run_for_server()` / `run_for_draft()` 签名不变
- `DraftEndpointConfig` 导出不变
- 3 个消费方文件零改动

### 4. main.py 添加 lifespan

```python
from contextlib import asynccontextmanager
from app.executors.factory import remote_executor

@asynccontextmanager
async def lifespan(app: FastAPI):
    yield
    await remote_executor.shutdown()

app = FastAPI(title="LLM Finetune API", lifespan=lifespan)
```

### 5. QueuedRemoteSession 的 Lock

保留 `QueuedRemoteSession.lock` 不变。池化并发由 `SessionPool.acquire/release` 控制，Lock 作为防御层。

## 状态流转图

```
┌─────────────────────────────────────────────────────────────────────┐
│  SessionEntry 生命周期                                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  warmup() / acquire() 扩展                                          │
│       │                                                             │
│       ▼                                                             │
│  ┌─────────┐     acquire()      ┌──────────┐     release()     ┌─────────┐
│  │  idle   │ ────────────────→  │  in_use  │ ──────────────→   │  idle   │
│  └─────────┘                    └──────────┘                    └─────────┘
│       │                              │                              │
│       │  health_check fail           │  release() + unhealthy       │
│       ▼                              ▼                              │
│  ┌──────────┐                  ┌──────────┐                   空闲超时
│  │ destroyed │ ←────────────── │ destroyed│ ←────────────── (total > min)
│  └──────────┘                  └──────────┘                         │
│       │                              │                              │
│       │  total < min → 补充          │  total < min → 补充          │
│       ▼                              ▼                              │
│  ┌─────────┐                   ┌─────────┐                          │
│  │  idle   │                   │  idle   │                          │
│  └─────────┘                   └─────────┘                          │
│                                                                     │
│  排队等待:                                                          │
│  ┌─────────┐   acquire() + total == max   ┌─────────┐              │
│  │ 请求到达 │ ──────────────────────────→  │ waiter  │              │
│  └─────────┘                              └─────────┘              │
│                                                 │                   │
│                                            release() 触发           │
│                                                 ▼                   │
│                                           ┌─────────┐              │
│                                           │ 执行请求 │              │
│                                           └─────────┘              │
└─────────────────────────────────────────────────────────────────────┘
```

## 并发时序图

```
时间轴 →

Server A (pool: 5 sessions)

Request 1 (train):  [acquire S1] ████████████████████████ [release S1]
Request 2 (status): [acquire S2] ██ [release S2]
Request 3 (log):    [acquire S2]    ██ [release S2]     ← S2 归还后被 R3 复用
Request 4 (eval):   [acquire S3] ████████ [release S3]
Request 5 (sync):   [acquire S4] ███ [release S4]
Request 6 (config): [acquire S5] █ [release S5]

→ R1 训练期间，R2-R6 使用 S2-S5 并行执行，互不阻塞

超出 max 的场景 (pool: max=5, 全部 in_use):

Request 7: [waiter ▓▓▓] ... [R2 release S2 → 触发 waiter] [acquire S2] ██ [release]
           └─ 排队等待 ─┘
```

## 验证方案

### 单元测试

在 `backend/app/tests/test_session_pool.py` 中：
1. 使用 `FakeTransport`（已有）测试池的 acquire/release 循环
2. 测试并发 acquire 不超过 max_size
3. 测试超时等待和 waiter 信号
4. 测试不健康会话的自动替换
5. 测试空闲缩容（mock time）

### 集成验证

基于已有 benchmark 脚本扩展：
1. 创建 `SessionPool(min=5, max=20)`
2. warmup 并验证 5 个会话就绪
3. 并发发送 10 个请求，验证全部完成且结果正确
4. 并发发送 25 个请求（超出 max），验证 20 个执行 + 5 个排队
5. 等待 1 分钟，验证探活正常
6. shutdown 并验证所有 terminal 被清理

### 回归验证

1. 启动后端服务
2. 通过前端执行服务器探测 → 验证 probe 正常工作
3. 通过前端执行脚本下发 → 验证 execution 正常工作
4. 检查 pool stats 端点 → 验证池状态正确
