# 部署配置文件
# 修改此文件适配不同服务器环境

# 服务器连接
REMOTE_USER=root
REMOTE_HOST=36.133.115.87
REMOTE_PASS='rnmjRG@56'

# 部署路径（服务器上的项目根目录）
DEPLOY_DIR=/home/admin/llm-finetune

# 服务端口
NGINX_PORT=30000
BACKEND_PORT=30001

# Docker 配置
DOCKER_IMAGE=llama-factory:2026_02_08
CONTAINER_NAME=llm-finetune

# 模型目录（宿主机路径，挂载到容器内）
MODELS_DIR=/data/models

# Nginx 配置文件路径
NGINX_CONF=/etc/nginx/conf.d/finetune.conf
